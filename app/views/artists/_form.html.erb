

<%= form_for(@artist, :html => {:multipart => true}) do |f| %> <! --tells html form that there will be files included here-->
  <% if @artist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% @artist.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :photo %><br />
    <%= f.file_field :photo %>
    <%= f.hidden_field :photo_cache %>
    <%= f.text_field :remote_photo_url %>
    <%= image_tag(@artist.photo_url) if @artist.photo? %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
