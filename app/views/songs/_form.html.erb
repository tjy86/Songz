<%= form_for(@song, :html => {:multipart => true}) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :photo %><br />

    <%= f.file_field :photo %>
    <%= f.hidden_field :photo_cache %>
    <%= image_tag(@song.photo_url) if @song.photo? %>
    <%= f.text_field :remote_photo_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
