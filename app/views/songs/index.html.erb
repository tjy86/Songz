<h1>Songs</h1>

<div class="index">
<% @songs.each do |song| %>
    <div class="index-box grid_12">
      <div class="grid_2"><%= image_tag song.photo %></div>
      <div class="buttons grid_10">
      <span class="index_name"><%= link_to song.name, song %></span>
      	<%= link_to 'Edit', edit_song_path(song) %>
      	<%= link_to 'Destroy', song, method: :delete, data: { confirm: 'Are you sure?' } %>
      </div>  
    </div><!--.index-box-->
  <% end %>
</div>